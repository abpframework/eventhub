apiVersion: v1
kind: Service
metadata:
  labels:
    name: {{ .Release.Name }}-{{ .Chart.Name }}
  name: {{ .Release.Name }}-{{ .Chart.Name }}
spec:
  type: LoadBalancer
  ports:
    - name: postgresql
      port: 5435
      targetPort: 5432
  selector:
    app: {{ .Release.Name }}-{{ .Chart.Name }}

